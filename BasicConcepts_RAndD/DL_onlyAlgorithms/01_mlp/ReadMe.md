# Multilayer Perceptron (MLP)

**Goal:** Learn XOR using a simple fully connected network.

**Instructions:**
1. Install PyTorch (`pip install torch`)
2. Run `python mlp_xor.py`
3. Observe predictions after training